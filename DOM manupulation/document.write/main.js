const result = (form) => {
  form.answer.value = form.a.value * form.b.value;
};

// const d = (form) => {
//   form.answer.value = document.write(form.a.value * form.b.value);
// };

// const d = (form) => {
//   form.answer.value = window.alert(form.a.value * form.b.value);
// };